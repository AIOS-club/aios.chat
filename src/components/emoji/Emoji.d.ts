import { IconProps } from '@/components/icon-picker/IconPicker';

export interface EmojiProps {
  id?: string;
  size?: string;
  shortcodes?: string;
  skin?: number;
  icon?: IconProps
  className?: string;
}
